import CreateForm from "./CreateForm";

export default function CreateRecipe() {
    return (
        <main>
            <h2 className="text-primary text-center">Add a new recipe</h2>
            <CreateForm />
        </main>
        
    )
}